/**
 * Sim Result — visualize simulation results (from Supabase) + AI report.
 * Placeholder; the full page (charts + Supabase results + AI report) is built next.
 */
export default function SimResultPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Sim Result</h1>
      <p className="mt-1 text-sm text-text-muted">
        Visualize runs and generate AI reports. (coming online)
      </p>
    </div>
  );
}
