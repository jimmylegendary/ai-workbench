/** Module Design — reusable module library/editor (routes-and-screens.md). STUB. */
export default function ModuleDesignPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Module Design</h1>
      <p className="mt-1 text-sm text-text-muted">
        Library + editor for reusable modules (workload fragments, serving configs, HW subtrees).
      </p>
      <p className="mt-4 font-readout text-xs text-text-muted">
        TODO: ModuleList · ModuleEditor · ModulePreview (see prototype-briefs/04-module-design.md)
      </p>
    </div>
  );
}
