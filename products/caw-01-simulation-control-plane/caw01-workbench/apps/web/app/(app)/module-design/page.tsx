import { ModuleDesignScreen } from "@/features/module-design/view/ModuleDesignScreen";

/** Module Design — reusable module library/editor (routes-and-screens.md). */
export default function ModuleDesignPage() {
  return <ModuleDesignScreen />;
}
