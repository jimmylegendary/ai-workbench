# See design/06-frontend/auth-and-supabase.md §5
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ENGINE_BASE_URL=http://localhost:8000

# Dev preview only — set to 1 to bypass the Supabase auth gate so the UI can be
# previewed before a real Supabase project exists. Leave UNSET in any real deploy.
PREVIEW_NO_AUTH=
